services:
  resume-site:
    image: nginx:alpine
    container_name: rohit-resume-container
    restart: unless-stopped
    ports:
      - "8081:80"
    volumes:
      - ${COMPOSE_BASE_PATH}/resume-site/public:/usr/share/nginx/html:ro
